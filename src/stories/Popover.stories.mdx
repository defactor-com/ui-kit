import { Meta, Story, Props } from "@storybook/addon-docs/blocks";
import { Popover } from "../components/Popover";

<Meta title="V2/Popover/Documentation" component={Popover} />

## Popover

## Example with defaultPopoverItems

```jsx
import { Popover, defaultPopoverItems } from "@defactor/ui-kit";
import { Button } from "@mui/material";
import React, { useState } from "react";

const ExampleComponent = (): JSX.Element => {
  const [anchorEl, setAnchorEl] = (useState < HTMLElement) | (null > null);

  const handleOpenPopover = (event: React.MouseEvent<HTMLButtonElement>) => {
    setAnchorEl(event.currentTarget);
  };

  const handleClosePopover = () => {
    setAnchorEl(null);
  };

  return (
    <>
      <Button onClick={handleOpenPopover}>Open Popover</Button>
      <Popover
        items={defaultPopoverItems}
        anchorEl={anchorEl}
        onClose={handleClosePopover}
      />
    </>
  );
};

export default ExampleComponent;
```
